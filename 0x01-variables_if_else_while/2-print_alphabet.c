#include <stdio.h>
/**
* main - prints the alphabet in lowercase
*
* Decsription: prints the alphabet in lowercase
*
* Return: Always 0 (Success)
*
*/
int main(void)
{
int i = 20

srand(time(0));
n = rand() - RAND_MAX / 2;
while (i < 40)
{
putchar(i);
i++
}
return (0);
}
